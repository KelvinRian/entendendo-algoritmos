using EntendendoAlgoritmos._6.Grafo;

namespace EntendendoAlgoritmos._7.PesquisaEmLargura
{
    public class PesquisaEmLargura
    {
        public Grafo Grafo { get; set; }

        public PesquisaEmLargura(Grafo grafo)
        {
            Grafo = grafo;
        }

        public int ObterMenorCaminhoEntre(Vertice inicio, Vertice fim)
        {
            //var filaDePesquisa = Grafo.Conexoes[inicio];
            //var caminhoMinimo = 1;
            //var verticesVerificados = new List<Vertice> { inicio };
            //foreach (var vertice in filaDePesquisa)
            //{
            //    verticesVerificados.Add(vertice);
            //    if (vertice == fim)
            //    {
            //        return caminhoMinimo;
            //    }
            //}
            //return 0;
            return 0;
        }
    }
}
